# Roadmap

Agreed work we plan to do next. Details stay short on purpose.

## Agreed Work

- **Essentials BES sample fixture pack** — add a small anonymized real-world fixture (`essentials-bes-sample`) with redacted `messages.dat`, `intl`, and PBS excerpts plus golden migration outputs; support optional full-game paths through `test/fixtures.local.yml`.
